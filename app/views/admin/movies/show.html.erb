<% @page_title = "#{@movie.title}の詳細" %>

<h1><%= @page_title %></h1>

<div class="toolbar">
    <%= link_to "編集", [:edit,:admin, @movie] %> | 
    <%= link_to "削除", [:admin,@movie], method: :delete,
                        data: {confirm: "本当に削除しますか？"} %>
</div>


<%= render "body" %>