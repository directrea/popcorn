<% @page_title = "#{@movie.title}の編集" %>

<h1><%= @page_title %></h1>

<%= form_for [:admin,@movie] do |form| %>
    <%= render "shared/movie_form", form: form %>
    <div><%= form.submit %></div>
<% end %>